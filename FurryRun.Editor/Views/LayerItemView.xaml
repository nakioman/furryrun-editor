<Canvas x:Class="FurryRun.Editor.Views.LayerItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
        Background="Transparent" Width="{Binding CanvasWidth}" Height="{Binding CanvasHeight}" ZIndex="{Binding ZIndex}" Margin="{Binding Margin}">
    <Canvas.LayoutTransform>
        <TransformGroup>
            <ScaleTransform ScaleX="{Binding ScaleX}"/>
            <TranslateTransform X="{Binding TranslateX}" Y="{Binding TranslateY}"/>
            <RotateTransform Angle="{Binding RotateCanvas}"/>
        </TransformGroup>
    </Canvas.LayoutTransform>
    <Image Width="{Binding Width}" Height="{Binding Height}" Source="{Binding SpriteFullPath}" Margin="{Binding ImageMargin}"/>
</Canvas>
